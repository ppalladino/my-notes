# Firebase

## Firebase Functions

Serverless functions. To set up and configure, first install CLI

```
// Install firebase cli tools
$ npm install -g firebase-tools

// navigate to directory with the code
$ cd <somewhere>

// login to firebase - will open a browser for google login
$ firebase login

// init the project
$ firebase init

// deploy your functions (assuming you have any)
$ firebase deploy 
```

